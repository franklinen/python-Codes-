list = []
while True:
        line = raw_input('> ')
        float = float(line)
        if line == 'done':
                break
        for item in line:
        	liz = list.append(float)
        	Maxim = max(liz)
        	Minim = min(liz)
print Maxim
print Minim

